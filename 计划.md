# SciQuery v1 执行方案

## 1. 目标与边界

### 1.1 目标

- 输入：单篇论文 `md` 文件。
- 输出：结构化研究课题列表，包含课题标题、推演逻辑、3 步实施方案、证据片段、三模型评分、聚合结果、状态。
- 核心目标：从论文中抽取“可立即启动”的研究课题，而不是宽泛方向、泛泛展望或空泛口号。

### 1.2 v1 边界

- 只处理单篇论文，不做跨论文联合排序。
- 只处理文本，不启用多模态能力。
- 当前测试阶段默认启用 `full_scan_test`，对正文全文 chunk 进行候选抽取。
- 保留三模型独立评分。
- 取消独立的“JSON 修复阶段”，由全局归并模型直接输出严格 JSON。
- 先落地本地工件文件和轻量数据库结构，后续版本再扩展成服务化流水线。

### 1.3 v1 不做的事

- 不做跨论文聚合推荐。
- 不做学科专属权重。
- 不引入额外 embedding 服务；v1 先使用本地轻量相似度方案。
- 生产收敛模式暂不启用；优先验证全文全量抽取效果。

## 2. 固定模型分工

- `DeepSeek-V3.2`：按 chunk 生成候选课题。
- `NVIDIA-Nemotron-3-Super-120B-A12B`：对候选课题做全局归并，并直接输出严格 JSON 结构的 merged topics。
- `GLM-5`：评审模型 1，重点看证据一致性、推演严谨性。
- `qwen3.5-122b-a10b`：评审模型 2，重点看具体性、可操作性、结构完整性。
- `qwen3.5-397b-a17b`：评审模型 3，重点看新颖性、研究价值、课题保留价值。

## 3. 统一工程约束

- 所有模型调用统一封装为同一套 adapter。
- 除 `Prompt A` 外，其余模型输入输出全部走 JSON schema。
- `Prompt A` 固定输出结构化 Markdown，再由本地解析器归一化为候选 schema。
- 批处理链路统一关闭流式输出，避免 JSON 解析失败。
- 所有 API key 只从环境变量读取，不写入代码文件。
- 每个阶段保留 `raw_output`，用于追踪和复盘。
- 每个阶段都生成独立工件文件，支持断点续跑。
- 任一模型调用最多重试 2 次；两次失败后直接标记 stage failed。
- 所有评分模型必须使用同一套五维 rubric，避免自由打分漂移过大。
- 全文全量模式下，若单轮上下文放不下，必须采用分批、分轮、层级归并，不允许提前按主观规则丢弃 chunk。

## 4. 输入、输出与目录

### 4.1 输入

- `paper_id`
- `paper_path`
- `paper_markdown`

### 4.2 工件目录

每篇论文固定输出到：

```text
artifacts/{paper_id}/
  01_chunks.jsonl
  02_candidates_raw.jsonl
  03_candidates_deduped.jsonl
  04_clusters.jsonl
  04_clusters_rounds/
  05_merge_rounds/
  06_merged_topics.json
  07_topic_scores.jsonl
  08_final_topics.json
  run_meta.json
```

### 4.3 最终输出对象

最终只保留两类状态：

- `accepted`：可直接入库。
- `review`：需要人工复核。

`rejected` 只保留在中间结果和数据库中，不进入最终展示结果。

## 5. 阶段 1：论文预处理

### 5.1 section 切分

- 先按 Markdown 标题切 section。
- 记录完整标题路径 `section_path`。
- 标题路径示例：`["Introduction", "Limitations"]`。

### 5.2 chunk 切分

- 默认按段落切 chunk。
- 单段过长时继续按句子切分。
- 单个 chunk 建议控制在 `600~1800` 字符。
- 小于 `120` 字符的段落默认不独立成 chunk，除非包含明确 future work、limitation、gap、open question 表述。

### 5.3 chunk 优先级

高优先级：

- `summary`
- `outlook`
- `discussion`
- `conclusion`
- `abstract`
- `introduction` 中明确写问题、局限、空白的段落

中优先级：

- `results` 中异常现象或未解释结果段落
- `methods` 中局限、适用范围、失败案例段落

低优先级：

- 背景铺垫
- 纯实验过程描述
- 与研究课题生成关系弱的图表说明

### 5.4 预处理预算

- 当前测试模式不截断正文 chunk。
- 除空段、纯格式噪声、纯参考文献列表外，全部 chunk 进入生成阶段。
- `priority` 标签保留，但只用于后续分析和对比，不用于本轮测试裁剪。
- `section_path` 全量保留，不设置每个 section 的 chunk 上限。

### 5.5 chunk 字段

```json
{
  "paper_id": "paper_xxx",
  "chunk_id": "chunk_001",
  "section_path": ["Discussion", "Limitations"],
  "chunk_index": 1,
  "chunk_type": "paragraph",
  "priority": "high",
  "char_count": 842,
  "chunk_text": "..."
}
```

## 6. 阶段 2：候选课题生成

### 6.1 生成策略

- 对每个入选 chunk 固定调用 `DeepSeek-V3.2` 3 次。
- 三次调用使用不同温度，分别为 `0.7`、`0.9` 和 `1.1`。
- 每次调用最多返回 `2` 条候选课题。
- 单个 chunk 最多产出 `6` 条候选。
- DeepSeek 必须直接使用 [Prompt_A_候选生成.md](/c:/science/sciquery/prompts/Prompt_A_候选生成.md) 作为基础模板，不允许自由改写任务目标和输出结构。

### 6.2 原始输出格式

`Prompt A` 的原始输出不是 JSON，而是固定结构的 Markdown 课题块：

```markdown
### 📌 课题 1：[具体明确动作的短语]

- **推演逻辑**：...
- **具体实施步骤**：
  1. **第一步（准备/建模/设计）**：...
  2. **第二步（执行/推导/验证）**：...
  3. **第三步（分析/对比）**：...
```

每次 DeepSeek 调用的完整原文输出直接保留到 `02_candidates_raw.jsonl`。

### 6.3 Prompt A 解析与归一化

生成阶段结束后，必须使用本地解析器把 Markdown 课题块解析为候选对象。

解析规则：

- 课题标题：从 `### 📌 课题 N：...` 提取。
- 推演逻辑：从 `- **推演逻辑**：...` 提取。
- 三步实施：从编号 `1/2/3` 提取。
- `paper_id`、`chunk_id`、`section_path`、`generator_model`、`run_index`、`temperature` 由外层调用元信息补齐。
- `source_type` 不由 Prompt A 输出，使用 chunk 所在 section 和关键词规则推断。

### 6.4 归一化后的候选字段

```json
{
  "candidate_id": "cand_001",
  "paper_id": "paper_xxx",
  "chunk_id": "chunk_001",
  "generator_model": "deepseek-v3.2",
  "run_index": 1,
  "temperature": 0.7,
  "topic": "...",
  "normalized_topic": "...",
  "derivation_logic": "...",
  "implementation_steps": ["...", "...", "..."],
  "source_type": "explicit_future_work",
  "source_chunk_id": "chunk_001",
  "evidence_section_path": ["Discussion", "Limitations"],
  "raw_markdown_block": "...",
  "raw_output": "...",
  "valid_flag": true
}
```

### 6.5 生成阶段硬性校验

候选出现以下任一情况，直接标记 `valid_flag=false`：

- 缺少 `topic`
- `implementation_steps` 不是 3 步
- 缺少 `推演逻辑`
- 步骤明显空泛，例如只有“进一步研究”“深入分析”“开展实验”这类无对象动作

### 6.6 生成阶段预算

- 若论文共有 `N` 个有效 chunk，则 DeepSeek 最多调用 `3N` 次。
- 若每次调用按上限返回，则候选理论上最多为 `6N` 条。
- 当前测试模式不在全局层面对候选做数量截断，只做以下两类收缩：
- 删除 `valid_flag=false` 的候选。
- 删除同一 `chunk_id` 内语义和标题都完全重复的候选。
- 生成阶段结束后，全部有效候选进入聚类阶段。

## 7. 阶段 3：轻量预去重与聚类

### 7.1 v1 去重原则

v1 不依赖独立 embedding 服务，先使用轻量可开发方案：

- 标题规范化去重：大小写归一、去标点、去多余空格。
- `topic + derivation_logic` 做 `TF-IDF + cosine` 粗聚类。
- 粗聚类阈值固定为 `0.82`。

### 7.2 cluster 输出字段

```json
{
  "cluster_id": "cluster_001",
  "paper_id": "paper_xxx",
  "candidate_ids": ["cand_001", "cand_008"],
  "cluster_size": 2,
  "representative_topic": "...",
  "representative_logic": "..."
}
```

### 7.3 聚类阶段预算

- 当前测试模式不对 cluster 数量做人工截断。
- 若候选总数超过单轮计算窗口，按 `50` 条候选为一批做分批粗聚类。
- 第一轮先在批内聚类，得到局部 cluster。
- 第二轮再对局部 cluster 的代表项做全局聚类，直到 cluster 集合收敛。
- 所有中间轮次结果写入 `04_clusters_rounds/`。

## 8. 阶段 4：Nemotron 全局归并并直接输出严格 JSON

### 8.1 职责

- 接收 cluster 列表和源候选信息。
- 合并语义重复或接近的候选。
- 标准化课题标题。
- 合并推演逻辑与实施步骤。
- 从来源 chunk 文本中抽取最能支撑课题的证据片段，生成 `evidence_quotes`。
- 继承全部来源候选、来源 chunk、证据片段。
- 直接输出严格 JSON，不再追加独立 JSON 修复阶段。

### 8.2 归并输出字段

```json
{
  "topic_id": "topic_001",
  "paper_id": "paper_xxx",
  "merge_model": "nemotron-3-super-120b-a12b",
  "title": "...",
  "normalized_title": "...",
  "merged_derivation_logic": "...",
  "implementation_steps": ["...", "...", "..."],
  "source_candidate_ids": ["cand_001", "cand_008"],
  "source_chunk_ids": ["chunk_001", "chunk_004"],
  "evidence_quotes": ["...", "..."],
  "merge_notes": "...",
  "raw_output": "..."
}
```

### 8.3 严格 JSON 要求

- 顶层输出固定为 JSON 数组或固定对象。
- 所有字段必须完整。
- `implementation_steps` 固定为长度 3 的数组。
- `source_candidate_ids`、`source_chunk_ids`、`evidence_quotes` 必须是数组。
- 分值字段不允许在此阶段生成。
- 不输出解释性文字，不输出 markdown 包裹。

### 8.4 归并阶段预算

- 若最终 cluster 数量可放入单次上下文，则 Nemotron 单篇论文调用 `1` 次完成归并。
- 若 cluster 数量超出单次上下文，则按 `8~12` 个 cluster 为一组做分轮归并。
- 每轮归并先生成 partial merged topics，再对 partial merged topics 继续归并，直到结果收敛。
- 归并阶段不因数量过多而提前删除 cluster，只允许通过语义合并减少总数。
- 所有中间轮次结果写入 `05_merge_rounds/`，最终结果写入 `06_merged_topics.json`。

## 9. 阶段 5：三模型独立评分

### 9.1 评分模型

- 评审模型 1：`GLM-5`
- 评审模型 2：`qwen3.5-122b-a10b`
- 评审模型 3：`qwen3.5-397b-a17b`

三个模型独立评分，不互相暴露分数和评语。

### 9.2 统一评分维度

- `specificity`：具体性
- `feasibility`：可操作性
- `evidence_alignment`：证据一致性
- `novelty`：新颖性
- `value`：研究价值

每个维度范围固定为 `1.0 ~ 5.0`，保留一位小数。

### 9.3 统一评分锚点

- `5`：证据清楚、任务具体、可直接开做、价值明确。
- `3`：基本成立，但范围、步骤或证据仍需补充。
- `1`：表达空泛、证据不足或无法执行。

### 9.4 单模型总分公式

```text
overall =
0.25 * specificity +
0.25 * feasibility +
0.20 * evidence_alignment +
0.15 * novelty +
0.15 * value
```

### 9.5 单模型输出字段

```json
{
  "topic_id": "topic_001",
  "judge_model": "glm-5",
  "specificity": 4.3,
  "feasibility": 4.1,
  "evidence_alignment": 4.5,
  "novelty": 3.7,
  "value": 4.0,
  "overall": 4.14,
  "comment": "...",
  "raw_output": "..."
}
```

## 10. 阶段 6：评分聚合、否决与状态判定

### 10.1 聚合方式

为降低不同模型打分偏松偏严带来的影响，v1 默认使用中位数聚合，而不是直接平均：

- `final_specificity`：三个模型该维度分数的中位数
- `final_feasibility`：中位数
- `final_evidence_alignment`：中位数
- `final_novelty`：中位数
- `final_value`：中位数
- `final_overall`：三个 `overall` 的中位数

### 10.2 分歧指标

- `overall_spread = max(overall) - min(overall)`
- `evidence_spread = max(evidence_alignment) - min(evidence_alignment)`

### 10.3 否决规则

出现以下任一情况，直接判定为 `rejected`：

- 至少 `2` 个模型给出 `evidence_alignment < 3.0`
- 至少 `2` 个模型给出 `feasibility < 3.0`
- 缺少有效 `evidence_quotes`
- `implementation_steps` 不是严格 3 步
- 步骤无法直接执行，只有抽象表达，没有对象、数据、方法或产出

### 10.4 状态判定

- `accepted`：`final_overall >= 4.2` 且 `final_evidence_alignment >= 4.0` 且 `final_feasibility >= 4.0` 且 `overall_spread <= 0.8`
- `review`：未触发否决，且满足以下任一条件
- `final_overall >= 3.5`
- `overall_spread > 0.8`
- `evidence_spread > 1.0`
- `rejected`：其余情况或触发否决规则

### 10.5 聚合输出字段

```json
{
  "topic_id": "topic_001",
  "final_specificity": 4.2,
  "final_feasibility": 4.0,
  "final_evidence_alignment": 4.4,
  "final_novelty": 3.8,
  "final_value": 4.1,
  "final_overall": 4.1,
  "overall_spread": 0.5,
  "evidence_spread": 0.4,
  "status": "review",
  "veto_reason": null
}
```

## 11. 最终输出结构

```json
{
  "paper_id": "paper_xxx",
  "final_topics": [
    {
      "topic_id": "topic_001",
      "title": "...",
      "merged_derivation_logic": "...",
      "implementation_steps": ["...", "...", "..."],
      "evidence_quotes": ["...", "..."],
      "source_chunk_ids": ["chunk_001", "chunk_004"],
      "judges": [
        {
          "judge_model": "glm-5",
          "overall": 4.14,
          "comment": "..."
        },
        {
          "judge_model": "qwen3.5-122b-a10b",
          "overall": 4.0,
          "comment": "..."
        },
        {
          "judge_model": "qwen3.5-397b-a17b",
          "overall": 4.5,
          "comment": "..."
        }
      ],
      "aggregate_score": {
        "specificity": 4.2,
        "feasibility": 4.0,
        "evidence_alignment": 4.4,
        "novelty": 3.8,
        "value": 4.1,
        "overall": 4.1,
        "overall_spread": 0.5,
        "evidence_spread": 0.4
      },
      "status": "review"
    }
  ]
}
```

## 12. 数据库与字段定义

v1 推荐至少保留 6 张表。

### 12.1 `paper_chunks`

- `chunk_id`
- `paper_id`
- `section_path`
- `chunk_index`
- `chunk_type`
- `priority`
- `char_count`
- `chunk_text`

### 12.2 `topic_candidates`

- `candidate_id`
- `paper_id`
- `chunk_id`
- `generator_model`
- `run_index`
- `temperature`
- `topic`
- `normalized_topic`
- `derivation_logic`
- `implementation_steps_json`
- `source_type`
- `source_chunk_id`
- `evidence_section_path`
- `raw_markdown_block`
- `valid_flag`
- `raw_output`

### 12.3 `topic_clusters`

- `cluster_id`
- `paper_id`
- `candidate_ids_json`
- `cluster_size`
- `representative_topic`
- `representative_logic`

### 12.4 `merged_topics`

- `topic_id`
- `paper_id`
- `merge_model`
- `title`
- `normalized_title`
- `merged_derivation_logic`
- `implementation_steps_json`
- `source_candidate_ids_json`
- `source_chunk_ids_json`
- `evidence_quotes_json`
- `merge_notes`
- `status`
- `raw_output`

### 12.5 `topic_scores`

- `topic_id`
- `judge_model`
- `specificity`
- `feasibility`
- `evidence_alignment`
- `novelty`
- `value`
- `overall`
- `comment`
- `raw_output`

### 12.6 `topic_score_aggregates`

- `topic_id`
- `final_specificity`
- `final_feasibility`
- `final_evidence_alignment`
- `final_novelty`
- `final_value`
- `final_overall`
- `overall_spread`
- `evidence_spread`
- `status`
- `veto_reason`

## 13. 预算控制

### 13.1 当前测试模式的动态预算

- 设有效 chunk 数为 `N`。
- DeepSeek 调用次数：`3N`
- 候选理论上限：`6N`
- cluster 数量：由语义去重结果决定，不预设硬上限。
- Nemotron 调用次数：由分轮归并轮数决定，不预设固定值。
- 三模型评分调用次数：`3T`，其中 `T` 为最终 merged topic 数量。
- 当前测试模式不以固定调用次数截断论文内容。

### 13.2 单次调用推荐输出上限

- DeepSeek：`max_tokens <= 3072`
- Nemotron：`max_tokens <= 6144`
- GLM-5：`max_tokens <= 2048`
- qwen3.5-122b-a10b：`max_tokens <= 2048`
- qwen3.5-397b-a17b：`max_tokens <= 2048`

### 13.3 大文档处理

- 若单篇论文 chunk 数过多导致单轮生成、聚类或归并无法放入上下文，则按 top-level section 分批处理。
- section 分批只改变执行顺序，不删除任何正文内容。
- section 内先完成候选生成和局部聚类，再汇总到全局归并阶段。
- 只有在 API 或上下文硬限制触发时才切 section 批处理，不在测试阶段主动裁剪内容。

## 14. Prompt 划分

v1 固定 5 套 prompt：

- `Prompt A`：候选生成 prompt，供 DeepSeek 使用，模板文件为 [Prompt_A_候选生成.md](/c:/science/sciquery/prompts/Prompt_A_候选生成.md)。
- `Prompt B`：全局归并 + 严格 JSON 输出 prompt，供 Nemotron 使用。
- `Prompt C1`：GLM-5 评分 prompt，强调证据一致性和推演严谨性。
- `Prompt C2`：qwen3.5-122b-a10b 评分 prompt，强调具体性和可执行性。
- `Prompt C3`：qwen3.5-397b-a17b 评分 prompt，强调新颖性和研究价值。

`Prompt A` 必须直接使用模板文件，不允许改写“任务核心原则”和“输出格式”。运行时只允许填充 `paper_id`、`section_path`、`chunk_id`、`review_chunk` 四类变量。

`Prompt A` 的输出为结构化 Markdown，不是 JSON；必须先经过本地解析器归一化，再进入后续聚类与归并阶段。

三个评分 prompt 的字段、分值范围、输出 schema 必须一致，只允许关注重点不同。

## 15. 开发顺序

### 15.1 第一批实现

- Markdown section/chunk 解析器
- chunk 优先级打标
- `paper_chunks` 落盘

### 15.2 第二批实现

- DeepSeek adapter
- Prompt A 模板加载
- Prompt A Markdown 解析器
- 候选 schema 校验
- 同 chunk 精确去重

### 15.3 第三批实现

- `TF-IDF + cosine` 粗聚类
- 分批聚类与多轮聚类收敛
- `topic_clusters` 落盘

### 15.4 第四批实现

- Nemotron 全局归并
- Nemotron 分轮 partial merge
- Nemotron 严格 JSON 校验
- `merged_topics` 落盘

### 15.5 第五批实现

- 三模型评分 adapter
- 中位数聚合逻辑
- 否决规则
- `topic_scores` 和 `topic_score_aggregates` 落盘

### 15.6 第六批实现

- `08_final_topics.json` 生成
- SQLite 或等价轻量数据库落表
- 断点续跑与失败恢复

## 16. 验收标准

- 单篇论文可稳定跑完整条链路。
- 最终能产出 `08_final_topics.json`。
- 全文有效 chunk 都进入过生成链路，不因优先级规则被裁掉。
- 每个最终 topic 都有可追溯证据片段和来源 chunk。
- 每个最终 topic 都有严格 3 步实施方案。
- 每个最终 topic 都有 3 份独立评分结果。
- 每个最终 topic 都有聚合分数、分歧指标和最终状态。
- 全流程支持大文档分批与多轮归并。
- 任一阶段失败后，可从上一个已完成工件继续运行。

## 17. v1 结论

v1 的主线固定为：

`Markdown 全文切块 -> DeepSeek 基于 Prompt A 对每个 chunk 多次抽取候选 -> 同 chunk 去重 -> 分批/多轮聚类 -> Nemotron 分轮全局归并并直接输出严格 JSON -> GLM-5 / qwen3.5-122b / qwen3.5-397b 三模型独立评分 -> 中位数聚合与否决 -> 最终落盘`

该版本优先解决五个问题：

- 三模型评分保留
- 生成与评审分离
- 全文全量测试优先
- 全局归并和 JSON 结构化合并为一个阶段
- 通过分批和分轮控制上下文压力
- 结果可复核、可追踪、可断点续跑
